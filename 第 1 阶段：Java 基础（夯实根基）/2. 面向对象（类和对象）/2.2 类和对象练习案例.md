一组**循序渐进、偏实战**的「**类和对象**」练习案例。风格偏你最近在学的 Java 基础 + 运维/后端视角，不玩空中楼阁 

---

## 一、入门热身：定义类 + 创建对象

### 案例 1：服务器 Server

**需求**

* 定义一个 `Server` 类
* 属性：`hostname`、`ip`、`status`
* 方法：`start()`、`stop()`、`printInfo()`

### 示例代码

```java
public class Server {
    String hostname;
    String ip;
    String status;

    void start() {
        status = "RUNNING";
        System.out.println(hostname + " 启动成功");
    }

    void stop() {
        status = "STOPPED";
        System.out.println(hostname + " 已停止");
    }

    void printInfo() {
        System.out.println("主机名：" + hostname + ", IP：" + ip + ", 状态：" + status);
    }
}
```

```java
public class Main {
    public static void main(String[] args) {
        Server s1 = new Server();
        s1.hostname = "web-01";
        s1.ip = "192.168.1.10";
        s1.status = "STOPPED";

        s1.start();
        s1.printInfo();
    }
}
```

**练习点**

* 类 vs 对象
* 属性存状态，方法管行为

---

## 二、构造方法 + this

### 案例 2：数据库连接 DBConnection

**需求**

* 使用构造方法初始化对象
* 使用 `this` 区分成员变量

```java
public class DBConnection {
    String url;
    String username;
    String password;

    public DBConnection(String url, String username, String password) {
        this.url = url;
        this.username = username;
        this.password = password;
    }

    void connect() {
        System.out.println("连接数据库：" + url + "，用户：" + username);
    }
}

public class Main {
    public static void main(String[] args) {
        DBConnection db = new DBConnection(
                "jdbc:mysql://localhost:3306/test",
                "root",
                "123456"
        );
        db.connect();
    }
}
```

**练习点**

* 构造器
* `this` 的真实作用

---

## 三、封装：private + getter/setter

### 案例 3：用户 User（非常经典）

```java
public class User {
    private String username;
    private int age;

    public void setUsername(String username) {
        if (username == null || username.isEmpty()) {
            System.out.println("用户名非法");
            return;
        }
        this.username = username;
    }

    public String getUsername() {
        return username;
    }

    public void setAge(int age) {
        if (age < 0) {
            System.out.println("年龄非法");
            return;
        }
        this.age = age;
    }

    public int getAge() {
        return age;
    }
}
```

```java
public class Main {
    public static void main(String[] args) {
        User u = new User();
        u.setUsername("admin");
        u.setAge(18);

        System.out.println(u.getUsername() + " - " + u.getAge());
    }
}
```

**练习点**

* 封装的意义：**不让外部乱改数据**
* setter 是“看门人” 🚪

---

## 四、对象数组 + 业务逻辑

### 案例 4：日志 LogEntry

**需求**

* 创建多个对象
* 遍历处理

```java
public class LogEntry {
    String level;
    String message;

    public LogEntry(String level, String message) {
        this.level = level;
        this.message = message;
    }

    void print() {
        System.out.println("[" + level + "] " + message);
    }
}
```

```java
public class Main {
    public static void main(String[] args) {
        LogEntry[] logs = new LogEntry[3];
        logs[0] = new LogEntry("INFO", "服务启动");
        logs[1] = new LogEntry("WARN", "内存使用率高");
        logs[2] = new LogEntry("ERROR", "连接失败");

        for (LogEntry log : logs) {
            log.print();
        }
    }
}
```

**练习点**

* 对象数组
* 模拟真实日志系统

---

## 五、进阶一点：对象之间的关系

### 案例 5：服务 Service + 配置 Config

```java
public class Config {
    String profile;

    public Config(String profile) {
        this.profile = profile;
    }
}
```

```java
public class Service {
    String name;
    Config config;

    public Service(String name, Config config) {
        this.name = name;
        this.config = config;
    }

    void start() {
        System.out.println("服务 " + name + " 使用配置 " + config.profile + " 启动");
    }
}
```

```java
public class Main {
    public static void main(String[] args) {
        Config config = new Config("prod");
        Service service = new Service("order-service", config);
        service.start();
    }
}
```

**练习点**

* 一个对象“持有”另一个对象
* 这是 Spring / 微服务的地基 🧱

---

## 六、给你 3 道「真·练习题」

1️⃣ 写一个 `DockerContainer` 类

* 属性：`name`、`image`、`running`
* 方法：`start()`、`stop()`

2️⃣ 写一个 `JvmProcess` 类

* 属性：`pid`、`memory`
* 方法：`printStatus()`

3️⃣ 写一个 `NacosService` 类

* 属性：`serviceName`、`group`
* 方法：`register()`（打印注册信息即可）

---

如果你愿意，下一步我可以帮你：

* 把这些案例**升级成接口 + 继承**
* 或做一个 **小型“Spring Boot 启动模拟器”**
* 或专门出一套 **运维转 Java 的对象模型练习**

你选一条，我继续往前铺路 🛤️
